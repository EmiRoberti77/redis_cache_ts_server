export enum PATH_ROOT {
  SERVER_ROOT = "/",
  API_ROOT = "/api",
  PROMPTS_ROOT = "/api/prompts",
}
export enum PATHS_PROMPT {
  ROOT = "/",
  ROOT_USER_PARAM = "/:user",
}

export enum PATHS_API {
  ROOT = "/",
}
